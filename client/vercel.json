{
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ],
  "build": {
    "installCommand": "npm install --legacy-peer-deps",
    "env": {
      "NPM_FLAGS": "--legacy-peer-deps"
    }
  }
}
